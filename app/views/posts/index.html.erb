<h1>Home Page</h1>
<ul>
<%= @posts.each do |post|%>
    <h3><%= link_to post.title, post_path(post) %> by <%= link_to post.user.name, user_path(book.user) %></h3>
    <%= post.picture %>
    <%= post.description %>
<%end%>
</ul>